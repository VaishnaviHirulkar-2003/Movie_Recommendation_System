<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 0;
      background: url('/your-dashboard-bg.jpg') no-repeat center center/cover;
      color: #fff;
    }

    form[name="frm"] {
      background: rgba(20, 20, 20, 0.95); /* your feature branch */
      padding: 30px 40px;
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.7);
      width: 100%;
      max-width: 460px;
      margin: 9% auto 0;
      backdrop-filter: blur(5px);
    }

    form[name="frm"] input[type="text"],
    form[name="frm"] input[type="password"],
    form[name="frm"] input[type="email"] {
      width: 100%;
      padding: 12px 15px;
      margin-bottom: 20px;
      border: 1px solid #444;
      border-radius: 8px;
      background-color: #121212;
      color: #eee;
      font-size: 14px;
      transition: border 0.3s, background-color 0.3s;
      box-sizing: border-box;
    }

    /* Focus state - combining both branches */
    form[name="frm"] input[type="text"]:focus,
    form[name="frm"] input[type="password"]:focus,
    form[name="frm"] input[type="email"]:focus {
      border-color: #0f91ff; /* from HEAD */
      background-color: #1e1e1e; /* from HEAD */
      outline: none;
    }

    form[name="frm"] input::placeholder {
      color: #aaa;
    }

    form[name="frm"] input[type="submit"] {
      width: 100%;
      padding: 12px;
      background: linear-gradient(to right, #2600ff, #ff5722); /* your feature branch */
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }

    form[name="frm"] input[type="submit"]:hover {
      background: linear-gradient(to right, #aa9fe7, #ff5722); /* your version */
      transform: scale(1.02);
    }

    h5 {
      text-align: center;
      margin-top: 2%;
      color: #44ff88; /* your version */
      font-weight: 500;
    }

    .maincon {
      width: 100%;
      height: 90.5vh;
      /* Uncomment and update below if needed
      background-image: url("https://www.boxofficepro.com/wp-content/uploads/2022/11/08_XD_Screen-1-scaled.jpg");
      background-size: cover;
      filter: brightness(1.4) contrast(1.3);
      background-repeat: no-repeat;
      background-position: center;
      */
    }
  </style>
</head>
<body>
  <form name="frm" action="/admin/updateadmin" method="post">
    <input type="hidden" name="userid" value="<%=msg.user_id%>" placeholder="">
    <input type="text" name="username" value="<%=msg.username%>" placeholder="">
    <input type="password" name="password" value="" placeholder="Enter Your Old Password">
    <input type="password" name="npass" value="" placeholder="Enter New password">
    <input type="email" name="email" value="<%=msg.email%>" placeholder="">
    <input type="submit" name="s" value="Update">
    <br>
    <h5> <%=genre%></h5>
  </form>
</body>
</html>
