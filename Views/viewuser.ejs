<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>User Admin</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      color: #eee;
      background: #1c1a1a; 
    }

    .moviecon {
      max-width: 1100px;
      margin: auto;
      background: #1e1e1e;
      border-radius: 10px;
      border: 1px solid #333;
      margin-top: 2%;
      padding: 10px;
    }
    .search-form {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 10px;
      margin-bottom: 20px;
    }

    .search-form input[type="text"] {
      padding: 10px 14px;
      border-radius: 6px;
      border: 1px solid #444;
      background-color: #2c2c2c;
      color: rgb(255, 255, 255);
      width: 300px;
      font-size: 14px;
    }

    .search-form input::placeholder {
      color: white;
    }

    .search-form button {
      padding: 10px 15px;
      background-color: #c54c1f;
      border: none;
      color: rgb(255, 255, 255);
      font-weight: bold;
      border-radius: 6px;
      cursor: pointer;
      transition: background-color 0.3s ease;
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .search-form button:hover {
      background-color: #d6bf72;
    }
    table {
      width: 100%;
      border-collapse: collapse;
    }

    thead tr {
      background: #2c2c2c;
      color: #f1f1f1;
      font-weight: 700;
      text-transform: uppercase;
    }

    th, td {
      padding: 12px 18px;
      text-align: center;
      border-bottom: 1px solid #333;
      font-size: 14px;
    }

    tbody tr:hover {
      background: #333;
      cursor: pointer;
    }

    td a.view-link {
      color: #bd522c;
      font-weight: 600;
      text-decoration: none;
      transition: color 0.2s ease;
    }

    td a.view-link:hover {
      color: #a0cfff;
    }

    td a {
      text-decoration: none;
      color: #bd522c;
    }

    td a button {
      background: #2c2c2c;
      border: 1px solid #444;
      color: #bbb;
      padding: 7px 12px;
      border-radius: 5px;
      cursor: pointer;
      font-size: 14px;
      transition: all 0.2s ease;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
    }

    td a button:hover {
      background: #d6bf72;
      border-color: #b96319;
      color: #fff;
      transform: scale(1.05);
    }

    td a button i.fa-trash-alt {
      color: #e24e18;
    }

    td a button i.fa-pen-to-square {
      color: #c05934;
    }
    .pagination {
      display: flex;
      justify-content: center;
      gap: 10px;
      margin-top: 20px;
    }

    .pagination a {
      padding: 9px 15px;
      background: #2c2c2c;
      border-radius: 6px;
      border: 1px solid #444;
      color: #bbb;
      text-decoration: none;
      font-weight: 600;
      transition: background-color 0.2s ease, color 0.2s ease;
      user-select: none;
    }

    .pagination a:hover {
      background: #481111;
      border-color: #f5f7fa;
      color: #fff;
    }

    .pagination a.active {
      background: #481111;
      border-color: #ede7e3;
      color: #fff;
      font-weight: 700;
      cursor: default;
    }
  </style>
</head>
<body>
  <div class="moviecon">

    <!-- Search Form -->
    <form class="search-form" method="GET" action="/admin/usersearch">
      <input
        type="text"
        name="query"
        placeholder="Search by username or email..."
        required
      />
      <button type="submit">
        <i class="fas fa-search"></i> Search
      </button>
    </form>

    <!-- User Table -->
    <table>
      <thead>
        <tr>
          <th>Sr</th>
          <th>Username</th>
          <th>Email</th>
          <th>View</th>
          <th>Delete</th>
          <th>Update</th>
        </tr>
      </thead>
      <tbody>
        <% if (user.length > 0) { %>
          <% user.forEach((u, index) => { %>
            <tr>
              <td><%= (currentPage - 1) * perPage + index + 1 %></td>
              <td><%= u.username %></td>
              <td><%= u.email %></td>
              <td>
                <a href="/admin/viewProfileparticular?id=<%= u.user_id %>" class="view-link">
                  View
                </a>
              </td>
              <td>
                <a href="/admin/deleteuser?id=<%= u.user_id %>">
                  <button>
                    <i class="fas fa-trash-alt"></i>
                  </button>
                </a>
              </td>
              <td>
                <a href="/admin/edituser?id=<%= u.user_id %>">
                  <button>
                    <i class="fas fa-pen-to-square"></i>
                  </button>
                </a>
              </td>
            </tr>
          <% }); %>
        <% } else { %>
          <tr>
            <td colspan="6"><strong>No Data Available</strong></td>
          </tr>
        <% } %>
      </tbody>
    </table>

    <!-- Pagination -->
    <div class="pagination">
      <% if (currentPage > 1) { %>
        <a href="?page=<%= currentPage - 1 %>">&laquo;</a>
      <% } %>

      <% for (let i = 1; i <= totalPages; i++) { %>
        <a
          href="?page=<%= i %>"
          class="<%= currentPage === i ? 'active' : '' %>"
          ><%= i %></a
        >
      <% } %>

      <% if (currentPage < totalPages) { %>
        <a href="?page=<%= currentPage + 1 %>">&raquo;</a>
      <% } %>
    </div>
  </div>
</body>
</html>
