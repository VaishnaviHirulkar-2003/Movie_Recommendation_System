<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Delete Admin</title>
  <link rel="stylesheet" href="/Css/AdminDashBoard.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
<style>
  body {
    margin: 0;
    padding: 0;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: url('/your-dashboard-bg.jpg') no-repeat center center/cover;
    color: white;
  }


  .delete-container {
    background-color: rgba(20, 20, 20, 0.9);
    padding: 30px;
    border-radius: 12px;
    box-shadow: 0 0 20px rgba(0, 0, 0, 0.7);
    width: 400px;
    text-align: center;
    margin: 14% auto;
    backdrop-filter: blur(4px);
  }

  .delete-title {
    font-size: 22px;
    margin-bottom: 20px;
    font-weight: 500;
    color: #fff;
  }

  form {
    display: flex;
    flex-direction: column;
    gap: 15px;
  }

  input, select {
    padding: 12px;
    font-size: 15px;
    border-radius: 8px;
    border: 1px solid #444;
    background-color: #121212;
    color: #eee;
    transition: all 0.3s ease;
  }

  input:focus {
    border-color: #ff9800;
    background-color: #1e1e1e;
    outline: none;
  }

  input::placeholder {
    color: #aaa;
  }

  button {
    padding: 12px;
    font-size: 16px;
    background: linear-gradient(to right, #2600ff, #ff5722);
    color: white;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.2s ease;
    font-weight: bold;
  }

  button:hover {
     background: linear-gradient(to right, #d2d0dd, #ff5722);
    transform: scale(1.03);
  }

  h4 {
    margin-top: 15px;
    font-weight: 500;
    color: #00ff88;
  }
  .maincon
  {
    height: 90.5vh;
  }
</style>
</head>
<body>
  <div class="delete-container">
    <div class="delete-title">Delete Admin</div>


    <form action="/admin/deleteadminapi" method="POST" >
      <input type="text" name="username" value="" placeholder="Enter the username">
    <input type="password" name="password" value="" placeholder="Enter the password">

      <button type="submit" onclick="return deleteadmin()">Delete</button>
      <h4><%=msg%></h4>
    </form>
  </div>


  <script>

    function deleteadmin()
    {
       return confirm("You sure want to delete");
    }
  </script>
</body>
</html>
